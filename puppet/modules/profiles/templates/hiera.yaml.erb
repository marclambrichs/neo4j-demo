---
# Managed by puppet
:backends:
  - yaml
:hierarchy:
  - "nodes/%{::trusted.certname}"
  - "%{::environment}"
  - "common"
:yaml:
  :datadir: "<%= @hiera_yaml_datadir %>/%{::environment}/hieradata"
